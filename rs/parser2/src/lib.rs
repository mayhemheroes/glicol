// the parser takes a str as input
// return a result type
// if everything is ok, the result should be a HashMap AST
// the error result can be parsing error or semantic error
// pasing error contains position and reason
// semantic error is audio/music related

// pub enum Node {

// }